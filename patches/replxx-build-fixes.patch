diff --git a/CMakeLists.txt b/CMakeLists.txt
index 915d80d..fec2a21 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -113,9 +113,10 @@ endif()
 target_link_libraries(replxx PUBLIC Threads::Threads)
 set_target_properties(replxx PROPERTIES VERSION ${PROJECT_VERSION})
 
-set_property(TARGET replxx PROPERTY DEBUG_POSTFIX -d)
-set_property(TARGET replxx PROPERTY RELWITHDEBINFO_POSTFIX -rd)
-set_property(TARGET replxx PROPERTY MINSIZEREL_POSTFIX)
+# Disable postfixes to avoid library naming issues
+#set_property(TARGET replxx PROPERTY DEBUG_POSTFIX -d)
+#set_property(TARGET replxx PROPERTY RELWITHDEBINFO_POSTFIX -rd)
+#set_property(TARGET replxx PROPERTY MINSIZEREL_POSTFIX)
 if ( NOT BUILD_SHARED_LIBS AND MSVC )
 	set_property(TARGET replxx PROPERTY OUTPUT_NAME replxx-static)
 endif()
diff --git a/src/conversion.hxx b/src/conversion.hxx
index 6587ad0..f9f5b47 100644
--- a/src/conversion.hxx
+++ b/src/conversion.hxx
@@ -11,7 +11,11 @@
 
 #if ! ( defined( __cpp_lib_char8_t ) || ( defined( __clang_major__ ) && ( __clang_major__ >= 8 ) && ( __cplusplus > 201703L ) ) )
 namespace replxx {
+// Suppress C++20 compatibility warning - char8_t is defined in replxx namespace only
+#pragma GCC diagnostic push
+#pragma GCC diagnostic ignored "-Wc++20-compat"
 typedef unsigned char char8_t;
+#pragma GCC diagnostic pop
 }
 #endif
 
